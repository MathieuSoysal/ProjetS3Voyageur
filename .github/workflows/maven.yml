name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: build with Maven
      run: mvn verify sonar:sonar -Dsonar.projectKey=MathieuSoysal_ProjetS3Voyageur -Dsonar.organization=mathieusoysal -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=3877876b9e3317775ea24323e9874f86bfb6121a
